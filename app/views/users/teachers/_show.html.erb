<div class="row">
  <div class="col-md-3">
    Welcome
    <h1><%= current_user.profile.first_name %></h1> You log in as admin.
  </div>
  <div class="col-md-9">
    <div class="panel panel-primary">
      <div class="panel-heading">
        Manage users
      </div>
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>User Id</th>
                <th>Image</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Role</th>
                <th>Action</th>
              </tr>
            </thead>
            <tbody class="table-hover">
            <%= render 'users/students/list' %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>








<%= render 'pairs/form' %>
<%= render 'pairs/admin' %>
