<% if !user_signed_in? %>

<p><%= link_to "Sign Up", new_user_registration_path %></p>

<% else %>

<div class="jumbotron">
  <h1>SuperMatch</h1>
  <p>This is just a demo</p>
  <%= link_to 'Go to your dashboard', user_path(current_user.id), class: "btn btn-primary btn-lg" %>
</div>




<% end %>
